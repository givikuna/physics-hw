#lang racket

; The law of reflection has the very simple form
; theta_incident = theta_reflected

; Part A

; If the light strikes the first mirror at angle theta1
; What is the reflected angle theta_2

; Express your answer in terms of theta_1

(displayln "a) θ_1")

; Part B

; Now find the angle theta3 (as shown in the image) in terms of theta_2
; Then substitute the expression from Part A

; Express your answer in terms of theta1 and any numerical angles.
; When entering numerical values, assume the value is in degrees, so just use the number "23" to indicate 23 degrees

(displayln "b) 90-θ_1")

; Now find the angle theta4 shown in the figure in terms of theta1 and alpha

(displayln "c) 90+θ_1-α")

; Now find theta6 in terms of theta1 and alpha
; assume theta4<90degrees

; theta_6 = theta_5
; theta_5 = 90 - theta_4
; theta_4 = 90 + theta_1 - alpha
; theta_5 = 90 - (90 + theta_1 - alpha)
; theta_5 = alpha - theta_1
; theta_6 = alpha - theta_1

(displayln "d) α-θ_1")
